/**
 * Enum to avoid "magic numbers" from MenuItems return values
 *
 * @author Johan Rodriguez
 * @version 1.0
 *
 * last modification: 29/09/2022
 */

#pragma once

// CommandCodes enumeration to avoid magic numbers from return values
enum class CommandCodes { CONTINUE, EXIT };
